# Development/test install (kept as the default "everything" set).
# Production deploys should install from requirements.prod.txt instead.
-r requirements.prod.txt

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-playwright>=0.4.0

# Development
ruff>=0.8.0
pre-commit>=4.0.0
